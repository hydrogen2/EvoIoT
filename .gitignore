edge-agent/bacnet-simulator/

# Environment files with secrets
.env
**/.env
__pycache__/

# Built binaries
platform/bento-custom/bento-custom.exe
platform/bento-custom/bento-linux

# claude-shim runtime files (secret token, logs)
platform/claude-shim/.token
platform/claude-shim/*.log

# Real project material (customer data) - not for the repo
tests/projects/
